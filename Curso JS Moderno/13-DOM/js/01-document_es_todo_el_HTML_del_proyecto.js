
// Para seleccionar todo el documento 

let elemento;

// Muestra todo el documento HTML
elemento = document;

// muestra en una lista todas las etiquetas del doc
elemento = document.all;

// muestra solo los elementos del head
elemento = document.head;

// muestra solo los elementos del body
elemento = document.body;

// muestra el dominio donde esta el documento
elemento = document.domain;

// muestra cuantos formularios tiene el doc
elemento = document.forms;

// muestra el primer formulario 
elemento = document.forms[0];

// muestra el id del primer formulario
elemento = document.forms[0].id;

// el metodo de envio POST o GET
elemento = document.forms[0].method;

// las clases que uso el formulario
elemento = document.forms[0].classList;

// la accion que va tener el formulario
elemento = document.forms[0].action;

// mustra los enlases que tiene el documento
// elemento = document.links;
// elemento = document.links[4];

// las clases del link en forma de arreglo
// elemento = document.links[4].classList;

// las clases del link en forma de string
// elemento = document.links[4].className;

// muestra las imagenes
// elemento = document.images;

// muestta los script del documento
// elemento = document.scripts;



console.log(elemento);