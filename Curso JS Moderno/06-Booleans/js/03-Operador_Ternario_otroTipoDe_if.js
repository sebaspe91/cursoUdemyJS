// comprar en un if

// cuando la variable lleva un true no hay necesidad de compararlos con === true solo con la variable

const autenticado = true;

if (autenticado) {
    console.log('Si puedes ver Netflix');
} else {
    console.log('No puedes ver Netflix');
}


// Operador Ternario

console.log(autenticado ? 'si esta autenticado' : 'No esra autenticado');